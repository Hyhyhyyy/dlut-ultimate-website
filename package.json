{
  "name": "DUT-ultimate-website",
  "version": "1.0.0",
  "description": "大连理工大学开发区校区极限飞盘队网站",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test tests/*.test.cjs",
    "qrcodes:generate": "node scripts/generate-site-qrs.mjs",
    "verify:live": "node scripts/verify-live-site.mjs",
    "check": "npm run test && npm run build",
    "cloudbase:login": "tcb login",
    "cloudbase:verify": "node scripts/check-cloudbase-env.mjs",
    "cloudbase:deploy:function": "npm run cloudbase:verify && tcb fn deploy member-api --yes",
    "cloudbase:deploy:web": "npm run cloudbase:verify && npm run qrcodes:generate && tcb app deploy --framework vite --force",
    "cloudbase:deploy": "npm run cloudbase:deploy:function && npm run cloudbase:deploy:web"
  },
  "keywords": [
    "ultimate-frisbee",
    "cloudbase"
  ],
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@cloudbase/js-sdk": "^3.6.4"
  },
  "devDependencies": {
    "@cloudbase/cli": "^3.6.4",
    "jsqr": "^1.4.0",
    "qrcode": "^1.5.4",
    "sharp": "^0.35.3",
    "vite": "^8.1.5"
  }
}
